const robo ={
    braços: 4,
    pernas: 2,
    arma: 'metralhadotra',
    armaEspecial: 'foguete'
}

let a = 'arma' // podemos acessar uma propriedade por meio uma variável( essa Forma não e muito utilizada)
console.log(robo[a]);